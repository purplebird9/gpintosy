基于你提供的 `loader.S` 代码，以下是这四个问题的标准答案。你可以将这些内容概括后写入你的 Design Document：

### 1. How does the bootloader read disk sectors? In particular, what BIOS interrupt is used?
*   **答案：** Bootloader 调用了 `read_sector` 子程序来读取磁盘扇区。
*   **关键点：** 它使用了 **BIOS 中断 `int 0x13`**，并将 `AH` 寄存器设置为 `0x42`（Extended Read 服务）。它通过在栈上构建一个“包”（Packet，包含缓冲区地址、读取扇区数、起始扇区号等），传递给中断处理程序来执行读取操作。

### 2. How does the bootloader decide whether it successfully finds the Pintos kernel?
*   **答案：** Bootloader 通过扫描磁盘的**分区表（Partition Table）**来寻找。
*   **关键点：** 代码在 `check_partition` 循环中检查分区表项：
    *   它检查分区类型字段（`es:4(%si)`）是否等于 **`0x20`**（这是 Pintos 定义的内核分区类型 ID）。
    *   它检查分区是否可引导（`es:(%si)` 的值是否为 **`0x80`**）。
    *   只有同时满足这两个条件，它才认定找到了 Pintos 内核。

### 3. What happens when the bootloader could not find the Pintos kernel?
*   **答案：** 如果遍历了所有硬盘分区仍未找到，它会报错并停止。
*   **关键点：** 代码执行到 `no_such_drive` 或 `no_boot_partition` 标签处，调用 `puts` 打印字符串 `"\rNot found\r"`，随后执行 **`int 0x18`**。`int 0x18` 是一个 BIOS 中断，通常用于通知 BIOS 启动失败，进而尝试下一个启动设备（例如尝试从网络启动或进入 BIOS 设置）。

### 4. At what point and how exactly does the bootloader transfer control to the Pintos kernel?
*   **答案：** 在加载完内核的所有扇区后，通过 `ljmp` 指令转移控制权。
*   **关键点：**
    *   **时机：** 在 `load_kernel` 的结尾，所有代码循环读取结束后。
    *   **方式：** 它首先从加载的内核 ELF 头中读取内核的入口地址，并将其放入内存位置 `start`。然后执行 **`ljmp *start`**（Long Jump，长跳转）。这会将 CPU 的指令指针跳转到内核的入口地址，从而将 CPU 控制权正式移交给 Pintos 内核。

---

**建议：** 在你的 Design Document 中回答这些问题时，最好标注出代码行数（例如：“在 `read_sector` 函数（Line 169）中...”)，这样助教能一眼看出你是通过阅读代码得出的结论。